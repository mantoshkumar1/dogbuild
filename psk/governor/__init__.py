"""Execution Governor — task-scoped action classification, policy, and audit.

Reduces unnecessary agent approval prompts without weakening safety.
The user defines the goal and risk boundary; DogBuild handles routine
execution decisions inside that boundary.
"""
